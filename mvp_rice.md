# MVP 범위와 RICE — 먼저 만들 기능 하나

MVP 목표는 사용자가 핵심 일을 한 번 끝낼 수 있는 가장 작은 작동 버전입니다.

|후보|사용자 문제|R: 같은 기간|I|C: 비율|E: 인시|점수|근거/추정·의존성|선정|
|---|---|---:|---:|---:|---:|---:|---|---|
|F-01|카드·계좌 여러 곳을 뒤져야 해서 지출 정리를 포기함(저니맵 최저점)|30명/월(추정)|3|60%(추정)|6(제대로 만들 때 기준)|9|근거: 저니맵 최저점(journey_map.md) / R·C는 추정|**선정**|
|F-02|앱을 처음 열었을 때 뭘 해야 할지 몰라 이탈함(진입 유도 문구)|30명/월(추정)|0.5|70%(추정)|1|10.5|추정|미선정(문구로만 반영)|
|F-03|한 번 쓰고 다음 달에 재방문 안 함(목표 설정)|10명/월(추정)|1|30%(추정)|4|0.75|추정, 의존성: 정적 페이지라 재방문 자체 측정이 어려움(가설)|미선정|

**계산식**
- F-01: 30명 × 3 × 0.6 ÷ 6시간 = 9점
- F-02: 30명 × 0.5 × 0.7 ÷ 1시간 = 10.5점
- F-03: 10명 × 1 × 0.3 ÷ 4시간 = 0.75점

> R·I·C·E는 모두 추정입니다(아직 실사용자 없음). E는 "제대로 만들 때" 기준 시간이며, 오늘 수업판(한 화면·약 45분)은 카테고리 3~4개·그래프 없이 표로만 보여주는 수준까지 줄입니다.

**점수와 별개 확인**

| |F-01|F-02|F-03|
|---|---|---|---|
|오늘 서버·키 없이 한 화면에서 되는가|✅|✅|△(재방문 측정 자체가 애매)|
|docs/topic.md의 데이터를 쓰는가|✅(D1 소비성향 67.2%)|❌|❌|
|가장 위험한 가정을 확인하는가|✅("가계부 안 쓴 이유=복잡함"을 저부담 입력으로 테스트)|❌|❌|

**선택**
- 점수 1등 기능: F-02(진입 유도 문구, 10.5점)
- 오늘 선택한 기능(F-01): 카테고리별 지출 입력 → 전국 평균 소비성향 비교
- 선택 이유(1등과 다름): F-02는 점수는 높지만 topic.md 데이터를 쓰지 않고 가장 위험한 가정도 검증하지 못하는 부가 문구 수준이다. F-01은 점수가 조금 낮아도 저니맵 최저점을 직접 해결하고, 오늘 확보한 데이터(D1)를 사용하며, "가계부를 안 쓴 이유는 복잡함 때문"이라는 가장 위험한 가정을 실제로 테스트할 수 있어 오늘 목적에 더 맞다.
- 오늘 미룬 기능: F-02(랜딩 카피 한 줄 정도로만 반영), F-03(다음 달 목표·재방문)

- 이번 핵심 기능: 카테고리별 지출 입력 → 전국 평균 소비성향 비교 결과 보기(F-01)
- 사용자가 완료할 행동: 카테고리별 지출 금액을 1개 이상 입력하고 결과 화면을 확인한다
- 입력 → 처리 → 결과: 카테고리별 지출 금액(원하는 주기로 입력) → 합계·카테고리 비중 계산 + 전국 평균(67.2%) 대비 내 소비성향 계산 → 카테고리 비중 표 + "평균보다 OO%p 높음/낮음" 결과 문구
- 이번에 제외할 기능: 계좌 연동, 자동 분류, 다음 달 목표 설정, 재방문 알림, 진입 유도 문구(F-02·F-03)
- 완료 기준: R-01 정상 입력 시 결과 표시, R-02 빈값 시 안내, R-03 오류(음수·문자) 시 안내까지 직접 확인
- 필요한 선행 연결: docs/requirements.md·qa_scenarios.md(M5-08)에서 R-01~R-03 정의 필요

RICE는 우선순위 도구이며 실제 사업 성공을 증명하지 않습니다. [원안: Intercom](https://www.intercom.com/blog/rice-simple-prioritization-for-product-managers/)
