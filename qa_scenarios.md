# 요구사항 기반 단위·통합 테스트 설계서

**M5 구현 전에 설계하고, 구현할 때마다 실행합니다.** 실제 결과와 통과 여부는 실행한 뒤 `test_log.md`에 기록합니다. 미실행은 통과가 아닙니다.

## 단위 테스트

|테스트 ID|요구사항 ID|대상 함수·동작|입력·사전 조건|기대 결과|실행 방법·명령|
|---|---|---|---|---|---|
|UT-01|R-01|`calcResult(income, categories)`|income=3000000, categories={식비:800000, 배달:400000, 쇼핑:900000, 교통:400000}|`{ total: 2500000, ratios: {식비:32.0, 배달:16.0, 쇼핑:36.0, 교통:16.0}, myRate: 83.3, diff: 16.1 }` 반환(diff는 내 소비성향 − 67.2)|브라우저 콘솔에서 `calcResult(3000000, {식비:800000,배달:400000,쇼핑:900000,교통:400000})` 직접 호출해 반환값 확인|
|UT-02|R-02|`validateInput(income, categories)`|income="", categories 4칸 모두 ""|`{ valid: false, message: "소득과 지출을 하나 이상 입력해야 결과를 볼 수 있어요." }`|콘솔에서 `validateInput("", {식비:"",배달:"",쇼핑:"",교통:""})` 호출|
|UT-03|R-03|`validateInput(income, categories)`|income=3000000, categories={식비:"-50000", 배달:0, 쇼핑:0, 교통:0}|`{ valid: false, message: "금액 칸에는 0 이상의 숫자만 입력할 수 있어요." }`|콘솔에서 `validateInput(3000000, {식비:"-50000",...})` 호출, 문자 입력("abc")도 동일 메시지인지 추가 확인|
|UT-04|R-04|`validateInput(income, categories)`|income=0, categories={식비:100000, 배달:0, 쇼핑:0, 교통:0}|`{ valid: false, message: "소비성향을 계산하려면 소득을 0보다 크게 입력해 주세요." }`|콘솔에서 `validateInput(0, {식비:100000,...})` 호출|

## 통합 테스트

|테스트 ID|요구사항 ID|연결 대상|사전 조건・입력|실행 순서|기대 결과|모의/실제 연결 구분|
|---|---|---|---|---|---|---|
|IT-01|R-01|`app.html` 전체 흐름|`app.html`을 더블클릭해 로컬로 연다|소득 3,000,000, 식비 800,000, 배달 400,000, 쇼핑 900,000, 교통 400,000 입력 → 「결과 보기」 클릭|화면에 "총지출 2,500,000원", 카테고리 비중 표(32.0/16.0/36.0/16.0%), "내 소비성향 83.3%, 전국 평균(67.2%)보다 16.1%p 높음", 하단 물가 참고문(+3.09%)과 출처가 표시됨|실제 연결(외부 API 없음, `data.js` 상수만 사용)|
|IT-02|R-02|`app.html` 전체 흐름|`app.html`을 로컬로 연다|아무 값도 입력하지 않고 「결과 보기」 클릭|"소득과 지출을 하나 이상 입력해야 결과를 볼 수 있어요." 안내만 나오고 결과 표·문구는 나오지 않음|실제 연결|
|IT-03|R-03|`app.html` 전체 흐름|`app.html`을 로컬로 연다|식비 칸에 `-50000` 입력, 나머지는 0 → 「결과 보기」 클릭|"금액 칸에는 0 이상의 숫자만 입력할 수 있어요." 안내가 나오고 결과는 나오지 않음|실제 연결|
|IT-04|R-04|`app.html` 전체 흐름|`app.html`을 로컬로 연다|소득 0, 식비 100,000 입력 → 「결과 보기」 클릭|"소비성향을 계산하려면 소득을 0보다 크게 입력해 주세요." 안내가 나오고 결과는 나오지 않음|실제 연결|

> 같은 흐름을 배포 주소에서 다시 확인하는 「배포 확인」은 M6-04에서 진행합니다.

교육용 예시가 아니라 이번 서비스에 맞춘 시나리오로 작성했습니다. 요구사항에서 기대값을 도출했으며, 구현 결과를 보고 기대값을 바꾸지 않습니다.

QA는 명세와 테스트의 누락·설명·오류 처리도 함께 점검합니다. 테스트 통과 수만으로 사용자 문제나 사업 가설이 검증됐다고 하지 않습니다.
