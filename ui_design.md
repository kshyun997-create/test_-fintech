# UI 설계 기록 · M5-08에서 먼저 정하고 M6-01에서 구현

> 다른 서비스의 로고·글꼴·이미지·화면을 복제하지 않습니다. `docs/ui_ref.md`의 원칙만 참고하고, 내 데이터·내 문장·원본 CSS/SVG로 만듭니다. 관찰용 캡처는 사용하지 않았습니다.

## 1. 참고 원칙과 근거

| 참고한 원칙 | 내 화면에서의 적용 | 참고 링크 |
|---|---|---|
| 한 화면·섹션에 한 메시지 | 랜딩은 섹션마다 제목 한 줄 + 핵심 문장 하나(AIDA 4섹션) | `docs/ui_ref.md` |
| 핵심 숫자는 크게 | 결과 화면에서 "내 소비성향 83.3%"와 "+16.1%p"를 가장 큰 글자로 | `docs/ui_ref.md` |
| 데이터는 출처·기준 시점과 함께 | 67.2%·3.09% 숫자 아래에 기관명·발표일·한계를 작게 표시 | `data_sources.md` |
| 오류는 입력 가까이 | R-02·R-03·R-04 안내를 해당 입력 칸 바로 아래에 표시 | `requirements.md` |

## 2. 랜딩 시각 설계 (AIDA)

|AIDA|랜딩에서 보여 줄 것|반드시 연결할 문서|사용자가 얻는 답|
|---|---|---|---|
|A · Attention|제목: "평균보다 얼마나 더 쓰고 있을까요?" + 숫자 2개(가구 평균소비성향 67.2%, 2025Q3 / 물가 +3.09%, 2026-08)와 출처·기간|`topic.md`, `data_sources.md`|왜 지금 이 문제를 봐야 하나|
|I · Interest|"카드값 보고 놀란 적 있나요?" — 페르소나(사회초년생, 계좌는 나눠뒀지만 정리는 안 함)의 불편 순간과 기존 대안(은행 앱 자동분류의 한계)|`persona.md`, `journey_map.md`|누구의 어떤 문제인가|
|D · Desire|"정확한 금액을 몰라도 됩니다" — 카테고리 총액만 원하는 주기로 입력 → 평균과 비교해 알려주는 흐름(전·후 3단계 도형)|`lean_canvas.md`, `mvp_rice.md`|왜 이 방법이 나은가|
|A · Action|입력 폼 미리보기(소득 1칸+카테고리 4칸) + "내 소비성향 확인하기" 큰 버튼 → `app.html`|`requirements.md`, 아래 3절|지금 무엇을 해 볼 수 있나|

- 마지막에 작게 표시: "오늘은 수업 데모입니다. 계좌 연동·저장 기능은 없으며, 입력값은 새로고침 시 사라집니다. 소득·지출 예시는 투자·재무 조언이 아닙니다."

## 2-1. 관찰용 레퍼런스 캡처 기록

|캡처 파일·원본 URL|관찰한 원칙|복제하지 않을 요소|내 화면에 적용한 원본 설계|
|---|---|---|---|
|사용자가 대화 중 공유한 토스 앱 실제 화면(로컬 채팅 첨부, 저장하지 않음)|핵심 숫자를 크게·설명은 작게 / 카드형 리스트(여백·둥근 모서리) / 색은 한두 가지로 절제|토스 로고·워드마크, 브랜드 색(블루), 정확한 화면 구성(알림벨·계좌 카드 배열·메뉴 그리드)|`index.html` 통계 카드에 흰 배경+부드러운 그림자+16px 라운드 적용(초록 단색), `app.html`(M6-02)의 결과도 같은 카드형 리스트로 설계 예정|

## 3. 앱 화면 설계 (`app.html`)

- 첫 화면에서 사용자 행동 하나만 보이게: 소득 입력 칸 1개 + 카테고리 지출 칸 4개(식비·배달·쇼핑·교통) + "결과 보기" 버튼 하나만 보인다. 결과 영역은 버튼을 누르기 전까지 비어 있다.
- 입력 순서와 각 입력의 예시: ① 이번 달 소득(예: 3,000,000) → ② 식비 → ③ 배달 → ④ 쇼핑 → ⑤ 교통(모두 원 단위 숫자, placeholder로 "0" 예시 표시)
- 정상 결과(R-01)에서 크게 보여 줄 값: "내 소비성향 83.3%"와 "전국 평균보다 16.1%p 높음"을 가장 크게, 그 아래 카테고리 비중 표(32.0/16.0/36.0/16.0%)와 총지출 2,500,000원, 맨 아래 작게 출처(가계동향조사 2025Q3·2025.11.27, CPI +3.09%·2026-08)
- 빈값(R-02)·오류(R-03)·경계값(R-04) 안내 문구: 결과 영역 자리에 표 대신 안내 문장 하나만 표시(빨간 텍스트), 각각 requirements.md에 적힌 정확한 문장을 그대로 사용
  - R-02: "소득과 지출을 하나 이상 입력해야 결과를 볼 수 있어요."
  - R-03: "금액 칸에는 0 이상의 숫자만 입력할 수 있어요."
  - R-04: "소비성향을 계산하려면 소득을 0보다 크게 입력해 주세요."
- 키보드·모바일에서 눌러 볼 요소: 숫자 입력 칸은 `inputmode="numeric"`으로 모바일에서 숫자 키패드가 뜨게 하고, "결과 보기" 버튼은 엔터 키로도 눌리게 한다.

## 4. 화면 요소 ↔ 요구사항 R-ID ↔ 확인 방법

| 화면 요소 | 연결 R-ID | 확인 방법(테스트 ID) |
|---|---|---|
| 소득·카테고리 입력 후 결과 표+문구 | R-01 | IT-01 |
| 빈 값 상태에서 "결과 보기" 클릭 | R-02 | IT-02 |
| 음수·문자 입력 후 "결과 보기" 클릭 | R-03 | IT-03 |
| 소득 0 입력 후 "결과 보기" 클릭 | R-04 | IT-04 |

다음: 이 설계대로 M6-01에서 `index.html`(랜딩)을 먼저 만들고, M6-02에서 `app.html`·`app.js`·`data.js`를 구현합니다.
